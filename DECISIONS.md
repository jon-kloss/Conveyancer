# DECISIONS

One line per judgment call beyond the docs — what, why, which doc section it touches. Review surface for the human.

- **Workspace layout:** `solver` crate holds both T0 and T1; `solver-wasm` is a thin cdylib wrapper over T0 for wasm-pack (SDD §2 names "solver-core → WASM"; a separate wrapper keeps good_lp/LP deps out of the wasm build). — SDD §2
- **Renderer directory:** React app lives in `renderer/` (not `app/`) to avoid colliding with the `app` crate name. — SDD §2
- **Dev bridge:** `app` crate ships two entry points — the Tauri shell and a `dev-bridge` HTTP+SSE binary exposing the identical command layer, so the renderer can be driven and screenshot in a headless container (no display) against the *real* Rust core. Tauri deps are feature-gated (`shell`, on by default; CI container builds it too now that WebKitGTK is present). Rust still owns canonical state in both modes; no state forking. — SDD §4, HANDOFF "verify as you go"
- **Map imagery:** no community tile set is bundled yet — licensing must be verified before ship (SDD §7). Phase 1 renders the documented placeholder treatment (diagonal-stripe = runtime asset placeholder) over the survey grid + biome ghost labels. **OPEN QUESTION for human: which community tile set / license to pursue.** — SDD §7, UI spec Assets
- **World-node snapshot:** bundled `world-nodes.json` is a hand-assembled subset (iron/copper/limestone/coal in the Grass Fields / Rocky Desert / Northern Forest areas, real-ish coordinates, correct purity distribution shape) sufficient for Phase 1 planning. Full community-data snapshot swap-in is tracked in BACKLOG (v1). — SDD §3 "static world data"
- **Docs.json fixture:** repo bundles a trimmed, UTF-8 Docs.json-format fixture covering the Modular Frame chain (+miners/belts/power) so tests and the dev app run without a game install; the parser detects UTF-16LE (real installs) vs UTF-8 (fixture) by BOM. — SDD §7
- **Internal factory wiring:** SDD §3 models inter-factory `Route`s but not intra-factory connections; the graph view needs them. Added `BeltEdge { factory, from, to, item, tier }` entity (endpoints = machine group or boundary port); flow rates on edges are derived solver output, tier is user-set. Same ◇◈◆ + flow grammar as routes. — SDD §3, UI spec screen 6
- **T0 count semantics:** "scaling counts/clocks/rates linearly" (SDD §5.1) implemented as: required group throughput scales linearly; count = ceil(throughput / machine base rate) with clock redistributed evenly (counts/clocks are numbers, not geometry, so Principle 5 holds). T1 does the same relax-round-redistribute per SDD §5.2.
- **Graph card positions:** added `graph_pos` to `MachineGroup`/`Port` (not in SDD §3) — "solves never move cards" (Principle 5) requires cards to *have* persisted positions; drag commits `move_group_card`/`move_port_card`. — UI spec screen 6
- **Later-phase surfaces omitted, not stubbed:** POWER/DIFF overlay chips, PLAN SUPPLY CHAIN button, advisor edge tab, audit drawer, and route drawing are absent (their subsystems are Phase 2/3/5). FLOWS/NODES keep their spec'd 1/4 key bindings so muscle memory transfers when the others land. — HANDOFF "Later-phase surfaces must not be stubbed"
- **Graph edge encoding on an all-planned factory:** graph belts render the Flow load encoding (color+width+pattern per mock 4a) with *italic* label chips carrying planned-ness; the "planned routes are always blueprint-dashed" rule (1e) applies to map routes, which arrive in Phase 2. Without this, Phase 1's all-planned factories could never show the exit criterion's "belt saturation coloring". — UI spec 1e vs screen 6
- **Alternate recipes render locked** (`NOT UNLOCKED`, 50% opacity) — there is no progression/unlock model until later phases; standard recipes are available. — UI spec screen 6 recipe strip
- **Slider ceiling magnetism:** within 1% of the binding ceiling the target snaps to it, so the hard stop + binding strip engage exactly ("tick at input ceiling — hard-stops there"). — UI spec screen 6
- **Map↔factory transition:** 400ms scale+fade rather than a shared-element zoom morph — keeps the cinematic beat cheap; reduced-motion collapses it globally. Revisit for the true morph in a polish pass (BACKLOG v1.1). — UI spec Motion
- **Claim flow:** claiming a node dispatches `claim_node` + `add_port` (input port with the extraction-rate ceiling) as one undo step; extractor picker offers Miner Mk.1–3. — SDD §3, mock 2a
- **Verification posture:** the Playwright exit-criterion suite drives the real Rust core through the dev-bridge; the Tauri shell compiles in CI but isn't interactively tested in the headless container (no display). First manual desktop smoke test is on the human. — SDD §11
- **Phase 1 exit tag:** annotated tag `phase-1` exists locally on commit `040ebd0` but the GitHub integration rejects tag pushes — the human should re-tag after merge (`git tag -a phase-1 040ebd0`). — HANDOFF commit discipline
- **Belt-style edges (user request):** graph edges now use a layout engine (`edgeLayout.ts`) — anchors spread deterministically along card faces (sorted by counterpart position), orthogonal runs with rounded corners like real conveyors, and schematic *hop* arcs where a belt crosses another. Anchors are visual only; connect-drag still uses the RF handles. — UI spec screen 6 spirit
- **Machine footprints (user request):** cards carry a top-down footprint strip (one shared px-per-meter scale, ≤12 outlines + overflow count) and the inspector shows `w×l m · ×n → total m²`. Dimensions are a community-documented table in `footprints.ts` — Docs.json has none; pak extraction (v2) can replace it. — SDD §7
- **Floors (user request):** `MachineGroup.floor` (serde-default 0, so existing plan files load unchanged) with `set_group_floor` command; context-bar F-chips filter (dim off-floor cards/belts), footer F-badges, inspector stepper; cross-floor belts render as lifts (⇅ on the label). Ports stay on the ground floor. — SDD §3 extension
- **Belt chip declutter (user request):** chips place collision-aware — candidates along the belt (long horizontal runs first), rejecting spots that overlap cards or already placed chips; longest belts place first. Belts shorter than 150 units compact the chip to the saturation % (full text on hover/selection) — a measured deviation from "always-present `n/capacity · %`" (1e): CRIT and selected belts always carry the full chip, so the alarm channel never compacts. — UI spec 1e
- **Floor aesthetics (user request):** floors read through structure, keeping the color law intact — (1) *floor plates*: translucent labeled hulls behind each floor's cards (`FLOOR 1 · 2 GROUPS · 1⤓ 2 IN`), rendered only when ≥2 floors exist; (2) *lift portals*: on a filtered floor, a cross-floor belt runs to a clickable square portal pin (square = infrastructure, A2.3) labeled `⤒/⤓ F<n> · %` that jumps to the connected floor with the belt selected; ghosted off-floor cards become non-interactive; (3) ALL view lift belts carry `⇅ F0→F1` chips and square lift pads at both ends. — A2.3 grammar, SDD §3 extension
- **STACK FLOORS (user request):** context-bar action arranging each floor's cards into its own horizontal band — highest floor on top, cutaway-elevation style — preserving intra-floor layout, snapped to the 16px grid, committed as one undoable step (plain `move_group_card` commands; the solver is untouched since geometry is user-owned, Principle 5).
- **AUTO-FLOOR (user request):** assigns floor = production stage (longest-path topological depth over the intra-factory group DAG; inputs low, final assembly high), then band-stacks — one undoable step of set_group_floor + move_group_card commands. Cyclic graphs bail without touching the plan (no stages to compute; loops arrive with fuel chains in later phases).
- **Belt junctions (user request):** `Junction` entity (splitter / smart splitter / programmable splitter / merger / storage) with the game's physical port budgets enforced on connect (splitter 1-in/3-out, merger 3-in/1-out, storage 1/1); standard junctions carry one item, smart/programmable may filter per output (the edge's item is the filter). Solvers treat junctions as pure per-item conservation nodes (Σin = Σout) — golden test proves they never change the math. Placed via the context bar + LOGISTIC menu; full floors/stack/auto-floor/undo/persistence support. — SDD §3 extension
- **Buildables catalog (user request):** gamedata now parses *every* `FGBuildable*` class from Docs.json into a generic `buildables` table (class, display name, native class) persisted in gamedata.sqlite and served to the renderer — anything the game data names can be displayed; specialized tables (machines/belts/extractors) stay the solver-relevant layer on top.
- **Empire recompute (Phase 2):** every edit runs one `empire_solve` pass — factories topo-sorted over belt routes, each solved with its In-port ceilings tightened to `min(canonical, upstream supply, belt tier cap)`, out-rates fed downstream, route manifests/flows written back into the same undo entry. A cycle in the route graph falls back to stable order and flags `empire_cycle`. Clamp write-back only applies to the port the user explicitly edited — an upstream dip surfaces as a DEFICIT row, never a silent rewrite of a downstream target. — SDD §12, Principle 2
- **Route paths are straight lines (Phase 2):** map routes render pin-to-pin (blueprint-dashed while ◇); the path array is already multi-point in the model, so waypoint editing can land later without a schema change (BACKLOG v1.1). — SDD §3
- **Power as pseudo-item (Phase 2):** `__PowerMW` (1 item/min ≡ 1 MW) with synthesized burn recipes per generator×fuel (`rate = MW·60/energy_MJ`) makes generation flow through the ordinary T0/T1 pipeline — the MW slider back-solves generator count + fuel exactly like items. Supplemental water is deferred with pipes (BACKLOG v1). — A2, SDD §5
- **Circuits are derived, not entities (Phase 2):** grids are recomputed union-find components over power-line routes each solve, named GRID A/B/… by member order; margin grammar OK ≥20% headroom / WARN 5–20 / CRIT <5. Persisted `Circuit` entities (naming, breakers) arrive with priority switches (BACKLOG v1). — A2.1
- **Power lines bind factories, not ports (Phase 2):** `add_route {kind: power}` joins two factory pins (power is a bus, A2.1) — no manifest, no tier; duplicates per pair rejected. The route popover offers "⚡ Power line" alongside belt candidates and never offers the MW pseudo-item as a belt.
- **Audit drawer (Phase 2):** TAB toggles a bottom HUD (46%, pinnable to 82%) with SATURATION (routes + intra belts, TRACE/UPGRADE TIER), DEFICITS (starvation + node conflicts), POWER (circuit margins + factory draws), PLAN DRIFT (empty until save import). Badges: hot belts ≥70%, deficit rows, non-OK circuits. — mock 2d, UI spec 1i
- **Leaflet stacking fix:** `.map-leaflet` gets `z-index: 0` so Leaflet's internal pane z-indexes (200–700) can't bleed over app chrome — found when factory pins rendered through the audit drawer.
- **Phase 2 exit tag:** annotated tag `phase-2` exists locally on the e2e-verified empire commit — like `phase-1`, the integration rejects tag pushes; the human should re-tag after merge. Exit criterion covered by `phase2-empire.spec.ts`: a 5-factory empire (ore → ingots → rods → screws → depot + coal plant) with live route saturation, deficits on upstream dips, and GRID A power margins. — SDD §12
- **Elevation groundwork (user request):** `MapPos` gains `z` (serde-default 0 — old plans load unchanged); world nodes gain `z` + `zone` (surface/cave) + `entrance` for cave nodes. Route length is now 3D and `DerivedRoute` carries climb up/down; factory elevation is planner-entered (SITE section in the summary drawer) since no heightmap is licensed. Pin drags preserve z; `move_factory_pin` refreshes the endpoint waypoints of every bound route so lines and lengths track re-siting. Belts ignore climb in solver math (correct per game); it becomes pump head-lift when pipes land and grade checks when rail lands. — SDD §3 extension
- **Cave nodes (user request):** rendered with an under-arc + `▾CAVE` label; hover/selection reveals the surface entrance (square = infrastructure, A2.3) with a dotted link, and the node drawer explains routing via the entrance. No terrain pathfinding — geometry stays user-owned (Principle 5).
- **Proposal aliases (Phase 3):** items carry the concrete commands they materialize to; cross-item references use `$alias` placeholder ids (Id is a string) resolved to real ULIDs at accept in dependency order — zero command-schema invasion, unresolved aliases abort before anything commits. — SDD §3/§10
- **Toggle cascade (Phase 3):** excluding a review row transitively excludes its dependents; re-including a row pulls its *dependencies* back in but never resurrects dependents — the cascade never guesses what the user wants back. — mock 3a
- **Consequence eval = scratch solve (Phase 3):** partial-accept recompute applies included items to a cloned PlanState, runs the ordinary empire solve, diffs (goal delta, power, new deficits, critical margins), and discards — one code path, no parallel arithmetic. — mock 3a
- **Staleness is derived (Phase 3):** proposals store an FNV-1a hash of the plan projection (proposals excluded, or drafting would self-stale); the STALE badge is `inputHash ≠ live planHash`. RESOLVE = re-solve into a new numbered draft; no stored stale flag to desync. — mock 3c
- **Recipe selection is per-item scoring, not MILP (Phase 3):** candidates scored min-machines-then-min-power independently per item — honest at fixture scale where alternates don't create cross-item trade-offs; true MILP lands when they do (BACKLOG). Alternates default OFF in the wizard (no unlock model; they render locked) and T2 suggests them explicitly, flagged NOT UNLOCKED.
- **A2.4 power sourcing (Phase 3):** the global solver appends generator expansion (SetPortRate on the MW port, sized demand × (1+margin cap)) + a power line when the empire has any generator; with no grid at all it logs the unsourced draw honestly instead of inventing a fuel chain with no claims. Max-new-sites currently caps at the single integrated site the solver produces; multi-site decomposition is backlogged.
- **Priority switches (Phase 3, A2.3):** `PrioritySwitch` sits ON a power route (square pin at its midpoint); shed order is P8→P1. SHEDS AT accumulates: switch s sheds when circuit demand exceeds generation + Σ downstream of earlier sheds; the load side of a cut is the half with less generation. Audit POWER tab gains PRIORITY/SHEDS AT rows + a BROWNOUT SIM row per grid. Deleting a line cascades its switches.
- **Phase 3 exit tag:** annotated tag `phase-3` local only (integration rejects tag pushes; human re-tags after merge). Exit criterion covered by `phase3-proposals.spec.ts`: wizard → reviewable proposal → partial accept with live recompute → accept as ONE undo step → undo restores everything. — SDD §12
- **Transport math (Phase 4, A3):** one `TransportParams` table drives rail/truck/drone; the inspector renders the full math block (travel × 1.12 terrain, dwell, editable 15% headway, RTT, throughput = units × slots × stack ÷ RTT) and the empire recompute uses that computed throughput as the route's capacity — throughput is computed, never asserted (Principle 10). Drones skip the terrain factor (straight-line by design). Signal-block capacity stays v2.
- **Cargo kinds interchange (Phase 4):** belt/rail/truck/drone all bind the same Out→In port pair, so `set_route_spec` swaps them freely (and edits consists/cars/dwell); power/pipe endpoints differ and never interchange. Rail manifests are single-item in v1 — multi-item manifests need multi-port binding (BACKLOG).
- **Import clustering (Phase 4, SDD §8):** DBSCAN eps 120 m, min_pts 1 over manufacturer+generator positions (save cm ÷ 100 → map meters); groups by (machine, recipe) with mean clock; names from the dominant output ("IRON INGOT WORKS 1"). Belt-bridged cluster merging deferred — belts' connection components aren't resolved in v1 (BACKLOG). Dunarr-076: 74k objects → 867 machines → 13 factories, 88 groups, <100 ms cluster+write.
- **Quarantine = non-vanilla paths (Phase 4):** worker quarantines Build_/BP_ classes outside /Game/FactoryGame/ (4,456 SML-modded objects in Dunarr-076), counted in the preview, never silently dropped. Vanilla decor (foundations, walls) is recognized-and-ignored, not quarantined.
- **Re-import accept syncs the Built layer (Phase 4):** drift items carry `SyncOp` payloads instead of commands; accepting a SaveReimport proposal updates ◆ built counts/groups directly — the ONE documented exception to "accept creates ◇ planned only", because the proposal records what already happened in the game. Still one undo entry.
- **Imported factories may not solve (Phase 4):** real saves reference the full recipe catalog; with the bundled fixture gamedata, unknown recipes leave a factory rendered (name, position, ◆ groups, counts) but unsolved (`solve_error`) — honest degradation until a real Docs.json install is pointed at (`FICSIT_DOCS_JSON`). Import stays enrichment, never load-bearing (Principle 6).
- **Phase 4 exit tag:** annotated tag `phase-4` local only (human re-tags after merge). Exit covered by `phase4-import.spec.ts`: Dunarr-076 imports as the built layer through the real worker+UI, one undo removes it, identical re-import reports IN SYNC, a divergent save renders drift rows in the DIFF (PLAN DRIFT) tab with REVIEW into the SaveReimport proposal — which is never auto-applied. — SDD §12
- **Advisor gating (Phase 5, SDD §9):** cards fire only when a condition BECOMES true (edge-detect on condition keys, not rules), at most once per rule per 30 s, never for a muted rule, never while paused. Dismiss = mute that rule, persisted — the anti-nag contract is "dismissing means stop telling me about this". Cards/mutes persist OUTSIDE the undo journal (they record what the advisor saw; undoing a plan edit must not eat them). Rules: node_conflict, new_deficit, saturation_high (≥75%), power_swing (<20% headroom), drift_detected (open SaveReimport proposal). Every CTA routes through existing review surfaces — the advisor never edits the plan.
- **AI offline is a first-class mode (Phase 5):** no `FICSIT_AI_KEY` → `AI OFFLINE` chip and the SAME heuristic feed; the hourly model-call budget (6/h) is displayed but unspent. Chat runs a deterministic engine offline: status questions answer from derived state with SAW provenance, and "produce <item> at <rate>/min" intents materialize through the SAME global-solver validation path a model's `proposal_intent` would — the model never mutates state either way, so offline and online differ only in prose. The streaming model relay is scaffolded behind the key check (BACKLOG).
- **Context snapshots are user-viewable (Phase 5):** `compact_state(scope)` is exactly what a model would receive — aggregated rollups, top-10 deficits, circuits — with the serialized byte size shown in the chat context bar and the full JSON behind ▸ VIEW. Empire snapshots stay well under the 30 KB target at current scale.
- **Style guides (Phase 5):** typed `StyleGuide` entity (palette/massing/techniques/sequence + source note) with create/delete/set-theme commands; factories link via the existing `styleGuide` field. The image→guide vision call is offline-gated: + NEW GUIDE creates an honest manual template noting "aesthetic inference needs a model key".
- **Icon pack stays placeholder (Phase 5):** `AssetSource` trait (Install/CommunityPack/Placeholder) landed with `PlaceholderAssets` only — the pinned community icon pack shares the SAME unresolved licensing question as the map tiles (human decision). The diagonal-stripe chip remains a first-class rendering, never an error state. — SDD §7
- **Onboarding is one card (Phase 5, mock 7c):** steps 1–2's install-path detection and icon-extraction theater don't apply while the catalog is the bundled fixture — the card states the active catalog source honestly (`FICSIT_DOCS_JSON` to re-extract) and offers the three doors (N/P/S). No tour; dismiss persists per plan.
- **Phase 5 exit tag:** `phase-5` local only (human re-tags after merge). The "survives a week of dogfooding without feeling naggy" criterion is qualitative — the mechanical guarantees behind it (edge-arming, debounce, persistent mutes, pause, badge-count-only presence) are unit-tested and e2e-verified in `phase5-advisor.spec.ts`; the week of dogfooding is yours. — SDD §12
- **A1 hard floor removed (post-review, user call):** the 1366×768 refuse screen measured CSS pixels, which locked out high-DPI displays whose *logical* resolution is small (4K TV at 300% OS scaling ≈ 1088×693). There is no minimum screen size anymore: below 1600 px everything degrades to the overlay layout, and the Tauri shell auto-zooms the webview out (to at most 60%) so the layout regains room while text stays physically large (`useAutoZoom`). The refuse card and `LayoutMode::refuse` are gone; error/hydrating screens keep the titlebar so the frameless window always has drag + min/max/close. — supersedes A1's "refuse gracefully" clause
- **Imported factories are auto-wired logically (user report, post-v0.1):** the save parser extracts machines but not belt connectivity, and an unwired factory solves as nothing — so `write_built_layer` wires each cluster by recipe I/O: producer groups edge into consumer groups per item, net consumption becomes In ports (surfacing as honest deficits until routed), net surplus becomes Out ports. This is logical structure, not the game's physical belt runs. Corollary invariant enforced in the same change: **the solver never resizes ◆ built groups** — T1 write-back skips `Status::Built` (import sync is the only writer of the built layer).
- **Layered graph auto-layout (user feedback, post-v0.1):** generated factories were grid-dumped and edge crossings made flow unreadable. New `planner-core::layout` — Sugiyama-lite: In ports column 0, groups/junctions ranked by flow depth (longest-path, cycle-tolerant), Out ports last, barycenter sweeps minimize crossings, rows stacked by real card heights. Imports lay out at creation; `TidyLayout` command (TIDY in the graph context bar) re-lays any factory as one undoable, cosmetic-only step — it applies to built cards too since positions aren't game truth.
- **Claim tethers on the world map (user feedback, post-v0.1):** node↔factory assignment was invisible outside the node drawer. Claimed nodes now tether to their owning pin: quiet dashed steel line; blueprint-dashed when the claim is ◇ planned; crit red when conflicted; selecting/hovering the factory or node promotes the tether to signal orange and names the factory at the midpoint ("orange is a verb" — highlight follows selection, never resting state). Legend row added.
- **Built-edit deltas (SDD §3.1.1, code-review M2):** `planned_delta` is realized as an inline `GroupDelta { count: Option<u32>, clock: Option<f64> }` — an overlay of planned *effective* values on the ◆ built baseline — not the SDD's literal `DeltaRef` entity (a referenced ◇ group composes only for count increases, would render as a phantom second card, and needs a new collection; the overlay handles increase, decrease, and clock retune uniformly, and undo comes free from the group's existing patch pair). Per-component `Option` so a clock-only retune never pins the count: unedited components keep tracking the baseline through re-import sync. `SetGroupCount`/`SetGroupClock` on ◆ write the overlay (setting the built value back clears that component; an empty overlay is `None`); baseline count/clock stay import-owned ground truth. The solver READS effective values but never WRITES deltas — deltas are user intent, materialized only by explicit commands; note the deliberate asymmetry: T1 redistributes counts/clocks for ◇ planned groups but leaves built-group deltas literal (a target drag never sprays ◇ deltas across an imported factory; solver-generated deltas are BACKLOG). Re-import sync writes the baseline and KEEPS the delta, clearing any component the game caught up to — the Inspector's "visible until built in-game" dissolves automatically. `SetGroupRecipe`/`SetGroupFloor`/`DeleteGroup` on ◆ stay rejected: those invalidate the baseline rather than overlay it (structural/demolition deltas are BACKLOG v1.1). No plan-file migration: every existing file serialized `plannedDelta: null`. — SDD §3.1.1
- **Polish pass batch 1 (user-driven, post-review):** map label declutter — pin chips, route/grid/switch label chips, and node ghost labels collision-cull deterministically (selected wins, hover reveals a culled pin chip; lines/pins always draw); parallel routes between the same factory pair fan into stable 9-px screen-space lanes shared by draw and hit-test (cargo endpoints normalize port→factory so mixed kinds share the fan); boundary In ports state their real source (FROM ROUTE / FROM NODE CLAIM / UNROUTED — SUPPLY ASSUMED — the old label lied on imported factories); backend error chips resolve ULIDs to display names; F-framing pads for the open inspector; audit rows name their destination; import preview's quarantine list is viewable.
- **Real world map + real nodes (user request):** the placeholder 12-node snapshot is replaced by the full vanilla dataset — 459 solid/oil nodes generated from a vendored MIT community extraction (`scripts/gen-world-nodes.py`, provenance in NOTICE.md) — and the map home renders the real in-game terrain under the data layer by default (TERRAIN chip / key 3 to toggle). The terrain is NOT a Leaflet pane: it's blitted as the bottom layer of the existing data canvas (pre-filtered once — saturate .5 / brightness .55 — so the data layer keeps owning color), because the canvas sits under the map-pane stacking context (pins above canvas) and any opaque pane content would occlude every canvas-drawn node/grid/tether. Same-canvas drawing also keeps terrain and data in lockstep on every pan/zoom frame. Fracking satellites, geysers, and cave/entrance zoning aren't in the dataset (BACKLOG); the schema and cave rendering stay.
- **Built-immutability matrix (code-review, planner-core-integrity):** tiers are physical game infrastructure, so `SetEdgeTier`/`SetRouteTier` now `require_planned` (consistent with DeleteEdge and SetRouteSpec — imported ◆ belts are corrected by re-import sync, not hand edits; tier-upgrade-as-planned-delta is BACKLOG alongside structural group deltas). `RenameFactory` stays deliberately unguarded: names are planner-side labels the save format doesn't know — import synthesizes them and re-import matching is positional, same reasoning as TidyLayout/card moves on ◆. `SetPortRate`/`SetPortCeiling` stay unguarded: targets/ceilings are solver intent, not save-file truth — guarding them would dead-end every imported factory. `ReleaseNode` unguarded is an acknowledged deferral until import creates ◆ claims. — SDD §3.1.1
- **Flow notation on the world map (user request, part 2):** route lines now carry map notation so kind + direction survive label culling — flow chevrons on every cargo route (path order = from → to), crossties on rail, cargo squares on trucks, dotted lines for drones; power lines stay glyph-free (a bus, not a flow). Label chips degrade instead of vanishing: when the full item·flow/cap·sat% chip can't fit, a tag-only micro-chip (MK.2 / RAIL / …) tries first — "level of each" stays readable in dense views. Node dots scale with zoom (3.5px at world view → 7px zoomed in; ghost labels drop at world zoom) so the 459-node field reads as background under factories and flows. Unknown item classes prettify (Desc_SteelIngot_C → STEEL INGOT) rather than leaking raw class names into chips.
- **Build queue is derived (W1c):** the "what do I do next" queue is a pure projection over canonical state (`derive_build_queue(state, gamedata)` in `crates/app/src/buildqueue.rs`), recomputed every empire solve like circuits/deficits — no ordering entity, no stored done-flag. A step is an existing ◇ planned factory (a rollup over its groups), a ◆ built group carrying a planned delta, or a planned route/claim; order is `(proposal number, entity ULID)` with manual entities in a MANUAL bucket (0). Completion is DERIVED through a shared `nearest_built_match` (250m, the same rule as re-import drift diffing, so "is it built yet" and "did the save move it" never disagree): a planned factory is ◆ Done when a built twin covers its (machine,recipe) groups, ◈ Partial if only some — ◈ is a rollup, NEVER written to `status` (that would trip `require_planned` and violate "◆ is game ground truth"). `accept_proposal` now stamps `CreatedBy::Proposal(pid)` on the ◇ entities it materializes (the raw commands default to Manual) so the queue buckets by proposal number and lights milestone progress; ◆ entities from drift sync keep their Import provenance. Milestone "built" is gross ◆ production of the item (the save has no lifetime-crafted counter). — SDD §8, mock 2d
- **Build override overlay (W1c):** the ONE new canonical, un-derivable, undoable state — a serde-default `build_overrides: BTreeMap<Id, BuildOverride>` collection + `Entity::BuildOverride` wired exactly like switches/styleGuides (+ `COLL_BUILD_OVERRIDES`), mutated by `SetBuildDone { id, done: Option<bool> }` through `state.upsert`/`remove` (Some → pin, None → clear) for one free undo entry; no `require_planned` (it's build-progress metadata, not a ◆ mutation). Resolution is `done = override ?? derived`. It is a SPARSE assertion overlay that lives outside the solver/drift reads, auto-dissolves on re-import (after a drift accept, drop overrides that now agree with derived — mirroring the planned-delta dissolve in import.rs), and is cascade-pruned when its step entity is deleted. Routes/claims can't auto-complete (the save carries machines, not belt connectivity) → manual-mark-only, labelled honestly "can't detect in-game".
- **Resume dashboard = auto-present overlay (W1c):** a dismissible overlay (same species as the audit drawer / advisor panel) that auto-presents ONCE per plan when there is work to resume (non-empty build queue OR open re-import drift), gated by a PERSISTED `viewState.resumeSeen` flag (like `onboarded`) so it greets once and never ambushes the restored map on later opens; H and a StatusBar RESUME chip reopen it on demand. It sits ON TOP of the restored map/factory view and reveals it unchanged on dismiss (Principle 1) — guarded off while a proposal review is open and on the empty-plan Onboarding. Content is pure projection: "what changed since last import" (a `last_import` META KV blob — session fact like view_state, written in `import_save`, surfaced through hydrate) + open drift REVIEW, half-built / starving / next steps (reusing the audit FIX WITH SOLVER / TRACE CTAs), and Done/total + a milestone bar. The hydrate effect is now ref-guarded to run once, because StrictMode's dev double-invoke would otherwise re-`set` viewState and clobber the freshly-persisted `resumeSeen`. — user decision (dashboard placement)
- **Refactor/cutover proposals (W2a):** a refactor is a new ◇ factory linked to the running ◆ it retires by a single serde-default `replaces: Option<Id>` on `Factory` (a planner-side LABEL, same species as `name` — never a ◆ mutation; set at accept by a trailing `SetFactoryReplaces { $newSite, oldId }` alias command the untouched accept path resolves, so accept stays ◇-only/one-undo and the ◆ is byte-identical). `SetFactoryReplaces` validates target-exists + `Status::Built` + not-self, no `require_planned` (RenameFactory's reasoning). The **cutover** and its **downtime** are DERIVED (`crates/app/src/cutover.rs`): `derive_cutovers` is a cheap presence/steps projection recomputed every empire solve beside the build queue — BuildNew (reuses the build queue / `nearest_built_match`), Switch (one manual step per item the old factory supplies, keyed on a synthetic `switch:<oldFid>:<item>` override — belt connectivity is invisible to the save), Dismantle (the old ◆; completion DERIVED = ◆ gone OR a `BuildOverride` pins it — "dismantle is INTENT, a referenced id, never a ◆ write"). The **downtime engine** (`Session::cutover_plan`, on-demand only, never in per-edit `empire_solve`) scratch-solves the whole empire at each phase boundary (clone-shape-`solve_all_readonly`-restore, reusing the eval_proposal pattern): `shape_for_boundary` removes the new ◇ until it's online (k≥2) and the old ◆ once the switch begins (k≥1), so the intermediate Switch boundary is the honest worst case (old down, new not up) and removal cascades through routes to capture the FULL downstream ripple — the reported per-item dip **rate** is computed/honest, only the wall-clock is a machine-count estimate (`SWITCH_MIN_PER_MACHINE`, rendered "(est)", Principle 10). Node reuse (new ◇ claims a node the old ◆ still holds) sets `downtime.hard` = unavoidable downtime and naturally lights the existing `DerivedNode.conflict`. `replaces` and cutover overrides auto-dissolve on re-import (`dissolve_stale_replaces` beside `dissolve_stale_overrides`; a removed ◆ cascade-prunes its dismantle override). Surfaced as `POST /api/cutover/plan` (drafts the Refactor proposal → review) + `/api/cutover/downtime` (scratch-solved), a Dashboard CUTOVER TIMELINE, a map REPLACES tether + RETIRING/INCOMING pin tags, and a PLAN REPLACEMENT action on a selected ◆. — top player pain: tearing factories down as tiers unlock
- **W2a hardening — honest downtime, clear infeasible notice:** cutover downtime distinguishes "no impact" from "can't compute" — when the old factory declares positive output but the scratch-solve baseline is ~0 (imported/unsolved/starved: bundled fixture catalog can't resolve its recipes), `cutover_plan` returns `downtimeAvailable = false` + a reason (set `FICSIT_DOCS_JSON`) instead of a silent-empty dips list that reads as "no impact"; the CUTOVER TIMELINE renders the reason as an ink note. Replacement infeasibility (node-budget) surfaces as a clear, dismissible warn notice at the PLAN REPLACEMENT button (with a multi-output "refactor one line at a time" hint), not just the terse status-bar chip — an expected planning outcome, not a fault. The player-facing output-selector (choose which product line to replace) is deferred to the multi-entity cutover model (BACKLOG).
- **W2b-A parser lift — unlocked schematics + resource-node context (data only):** the save parser now emits two new enrichment carriers on `ImportSnapshot`, consumed by later W2b sub-units (unlocked-alt awareness, node reconciliation, empire alt-optimizer); no wizard/solver/import behavior changes yet. (1) `unlockedSchematics: string[]` — the `BP_SchematicManager_C.mPurchasedSchematics` array reduced to schematic class names (`Schematic_1-2_C`, …); `[]` when the actor is absent. (2) per-extractor `nodeActorId` — the `mExtractableResource` object-ref pathName (`Persistent_Level:PersistentLevel.BP_ResourceNode109`), a stable level-instance id for re-match. Gamedata gains `GameData.schematics: BTreeMap<schematic class → unlocked recipe classes>` parsed from `FGSchematic.mUnlocks` (Recipe_* refs only); serde-default so the trimmed fixture (no FGSchematic) yields an empty map and loads unchanged. All new fields serde-default (old snapshots/plan files/fixtures load with no migration); tolerant — missing shapes degrade to `[]`/`None`, never a hard error. **PROBE OUTCOME (Dunarr-076.sav, `@etothepii/satisfactory-file-parser` v4.1.1):** the parser EXPOSES — the schematic manager + `mPurchasedSchematics` (schematic paths), the miner→node ref (`mExtractableResource`, present on 100% of miners/oil pumps/water pumps), and the clock (`mCurrentPotential`). It does NOT expose — node **purity** (`BP_ResourceNode_C` carries only `mResourcesLeft` + `mDoSpawnParticle`; purity is static map data the save doesn't duplicate), the **resource item** (Desc_… not on node or miner), or a clean **items/min extraction rate** (only productivity measurements + clock). → **Chosen purity approach for W2b-C: snapshot-primary purity** — bind each miner to a world node by position, keyed for stability on the save's `nodeActorId`, and read purity from the bundled world-nodes catalog (the trusted source); `resource`/`purity`/`extractionRate` are carried as `null`/absent from the save (honest degradation). — shared save-parser lift for W2b (B) unlocked-alt awareness, (C) node reconciliation, (D) empire alt-optimizer
- **W2b-B — unlocked alternates gate recipe eligibility:** alternate recipes gate on a save-derived unlocked-recipe set (a META KV fact `Session.unlocked`, resolved at import from `mPurchasedSchematics × FGSchematic` unlocks, persisted beside `last_import`, surfaced by hydrate as `unlocked` — NOT undoable, outside `plan_hash`); `pick_recipe`/`t2_optimize` gate per-recipe on `!alternate || unlocked.contains(class) || include_alternates`, so unlocked alts are first-class (no NOT-UNLOCKED flag) while `include_alternates` re-scopes to "also consider LOCKED alts (as suggestions)"; the trimmed fixture ships no schematics → unlocked resolves empty → alternates stay locked exactly as before (identical behavior, existing e2e unchanged).
- **W2b-C — node reconciliation (bind ◆ claims to save nodes + plan-local position overlay):** import now creates ◆ node claims (first time — closing the "import creates zero claims" gap): each attributed miner binds by position to the nearest free bundled world node (`NodeClaim.node` = snapshot id, `saveNodeId` = the save's stable `nodeActorId` for re-match), or, on no catalog match, mints a plan-local `save:<id>` claim. Positions reconcile through a `node_overrides: BTreeMap<String, NodeOverride>` overlay — the ONE new canonical, serde-default collection (`Entity::NodeOverride`, `COLL_NODE_OVERRIDES`, wired exactly like `build_overrides`), mutated by `SetNodeOverride { id, node_override: Option<NodeOverride> }` through `upsert`/`remove` for one free undo entry, no `require_planned` (plan-local metadata, not a ◆ mutation). A node's resolved geometry is `snapshot ⊕ override`; the bundled asset (and any `FICSIT_WORLD_NODES` catalog swap) stays an ambient default, never mutated. First-import binds silently (a miner >30 m from its bound node writes a silent ground-truth correction); a divergent re-import emits a reviewable `SyncOp::CorrectNodePosition` drift row (never auto-applied) that `apply_sync` writes into `node_overrides` on accept, then `dissolve_stale_node_overrides` drops overrides the save has caught back up to (within 30 m of the catalog) or whose claim is gone — mirroring the planned-delta / build-override dissolve. `DerivedNode` gains `drift` (override disagrees with the catalog); `conflict` stays double-claim only. **Purity is snapshot-primary (probe-confirmed the save carries no purity/rate)** — there is NO purity override field: the trusted catalog is the only source, never invented from the save. `FICSIT_WORLD_NODES` (`worldnodes::load()`, parse-or-fall-back-to-`bundled()` with a logged warning) swaps the ambient catalog under the plan-local overlay, plumbed like `FICSIT_DOCS_JSON`.
- **W2b-D — empire alt-recipe optimizer (advisory):** the empire-wide alternate-recipe optimizer is a DERIVED/ADVISORY greedy per-item ranking (`crates/app/src/altopt.rs::empire_optimize`, pure over `(state, gamedata, unlocked)`, no new canonical state) — for each product with an UNLOCKED alternate that isn't already chosen, it computes the adopt-everywhere delta (Σ machines/power/inputs current − alt, reusing the T2 machine/clock arithmetic + a PER-FACTORY `source_of` gate on planned groups so advertised savings == adoptable savings — built groups always count, re-sourcing through `plan_replacement`) and ranks LEXICOGRAPHICALLY by machines saved, then retool hours, then power saved, then input savings (no cross-unit arithmetic); the honest input trade (e.g. iron→steel) is surfaced, never hidden. Retool cost is a CHEAP machine-count estimate at ranking time (O(n), no scratch-solve): ◇ planned → ~0 (a legal in-place `SetGroupRecipe`), ◆ built → `cutover::est_hours(Σ built machine count)` + a hard `node_reuse` flag when a built factory still holds a node its refactored replacement would re-claim; the expensive per-boundary scratch-solve (`cutover_plan`) stays DEFERRED to the per-row drill-down. CTAs route to draft proposals in the existing ProposalReview and keep the contract pivot intact: an all-◇ opportunity drafts a T2 `SetGroupRecipe` proposal (`optimize_to_recipe`, legal on planned); ANY ◆ built factory drafts a W2a Refactor per built factory via `plan_replacement` (`Session::optimize_adopt` routes; the ◆ layer is NEVER mutated — test-asserted byte-identical). Surfaced as `GET /api/optimize/empire` (read-only ranking) + `POST /api/optimize/adopt`, and a new AUDIT drawer "ALT OPTIMIZER" tab. The trimmed fixture ships no unlocked alternates → the ranking is empty there (honest degradation; e2e asserts the tab renders empty without breaking the drawer), so the algorithm + CTA routing are proven by Rust unit tests over synthetic gamedata. MILP (cross-item trade-offs) + multi-factory single-cutover are BACKLOG. — the highest-value W2b sub-unit: alt recipes are a "slot machine", this answers empire-wide "adopt these 3 alts → save 42 machines / 310 MW, here's the retool plan"
- **BUILD SHEET — per-factory copy/print export (plan → game bridge):** a read-only per-factory BUILD SHEET renders the existing derived state (machines + effective count/clock, inputs + honest boundary-port source labels, outputs, routes + belt/pipe/rail tiers + item + rate + 3D length, total power) as a clean copy/print-friendly checklist — clipboard-copy (plain-text/markdown) + `@media print` CSS, no file-download dep; composed entirely in the renderer from store/derived state (no backend change — route `lengthM` was already exposed on `DerivedRoute`), never mutates the plan.
- **TRAIN ANSWER-SHEET — trains-needed before you build (task #49):** the "how many trains does this route need?" answer is surfaced BEFORE any rail is laid: TRAINS NEEDED = `ceil(demand ÷ per-train throughput)`, plus per-train throughput, consist/RTT/headway, and an honest SHORT-by-N/min vs surplus verdict in flow tokens. It reuses the P4 transport math read-only — the prospective popover calls a new read-only `route_calc` endpoint (session method + dev-bridge `/api/route/calc` + Tauri command) that resolves the two pins' distance server-side and folds the canonical `rail/truck/drone_math` through a Rust `train_answer(math, units, demand)` (ceil/surplus, unit-tested); the existing route inspector folds its already-derived math block via a trivial renderer mirror of the same ceil wrapper. No plan mutation from the calc. Clipboard export (`sheetToText`-style plain text) on both surfaces; the transport FORMULA is never forked (Rust owns it, both surfaces reuse it).
- **Recipe resolution is a memoized backtracking DFS (PR #7 review):** cycles are OUTCOMES — Infeasible naming the item and the alternates escape — never hangs and never degraded proposals; candidate cost order is (starved, cyclic narrowed to primary-product partners, throughput, power, alternate); validated against the real 1.0.0.5 catalog with a 23,800-run unlock-set sweep, 0 false outcomes. — SDD §10
- **Expansion cap is a combinatorial backstop only (PR #7 review):** `EXPANSION_CAP` 10_000 (real max legit ≈ 994 pops incl. alts); a cap hit yields Infeasible "did not converge", never a Proposal — accumulated demand is non-converged by construction.
- **is_resource raw gate + supply-assumed raws (PR #7 review):** any `is_resource` item short-circuits to a raw; a resource with zero snapshot nodes (water, nitrogen) is supply-assumed — no claim, no port, no gating — and the SITING log names the assumption.
- **Geothermal 200 MW is the normal-purity average (PR #7 review):** the game cycles ±50% and scales by geyser purity (×0.5/×2), which imports don't know; purity-scaled truth needs geyser nodes in the world snapshot (BACKLOG).
- **Wizard log renders a last-400-line window (PR #7 review):** older lines collapse to an "n earlier lines" note — scrollback carries no review-relevant decision.
- **Import modal header derives live from plan state, latched at run (PR #7 review):** IMPORT vs RE-IMPORT tracks the live plan (undo included) until the import actually runs, then latches for that run.
- **gamedata.sqlite cache keyed on (game_build, schema_version) (PR #7 review):** either mismatch — or an absent schema_version — forces re-parse (serde-default fields would silently read false from old blobs); schematics are not yet persisted (no table), so the cache stays write-only until both land. — SDD §7
- **Real icons + clearance footprints (user request):** the 64px community icon set (greeny/SatisfactoryTools — icons extracted from the game, © Coffee Stain Studios; NOTICE.md) is vendored by `scripts/fetch-icons.mjs` into `renderer/public/icons` and renders inside the existing chip tile for every class in `iconManifest.json`; a class off the manifest — or an `<img>` that fails to load — keeps the monogram chip (honest degradation, never a broken-image glyph). Machine footprints are now DERIVED from Docs.json `mClearanceData` (union bounding box over all clearance boxes, cm → m, serde-default `Machine.footprint_m`, cache schema v3) and surface on group cards, the inspector, and the BUILD SHEET ("· 8 × 10 m each" — foundation-pad sizing); the community table in `footprints.ts` demotes to fallback for catalogs without clearance data, and card outlines cap at 26 px so real giants (Nuclear Plant 36×42 m) don't swallow the card — a measured dent in the shared px-per-meter scale, named in the hover title. — SDD §7
- **MOTION = THROUGHPUT (user request):** an edge animates only when its derived flow rate > 0; idle edges are static; animation speed scales with utilization (clamped, ~4s trickle → 0.8s saturated); `prefers-reduced-motion: reduce` disables all of it. Color stays status-only (flow-ok/warn/crit) — motion is a new orthogonal channel carried by a neutral-ink moving-dash overlay (graph belts: a second `edge-flowing` SVG path animating `stroke-dashoffset`; map routes: a lightweight second canvas re-stroking ONLY flowing polylines at ≤24 fps, RAF loop alive only while a flowing route is visible, the tab is visible, and reduced-motion is off — the full data-canvas redraw stays event-driven). — UI spec 1e, A3.1
- **Flow EFFICIENCY grammar (user request):** belts don't jam — a ratio-perfect build runs its belts at 100% and that is OPTIMAL, so the flow bands flip from congestion- to efficiency-framing: flowing at ≤50% utilization = flow-warn amber "UNDER-USED" (over-built or starved upstream; 30 on a 60-belt is the canonical case), >50% INCLUDING a full belt whose demand is met = flow-ok green "GOOD", and flow-crit red = "BOTTLENECK" only on solver-named evidence — a shortfall or clamped target ceiling whose binding names that belt (graph), or a deficit registered through a route running at full capacity (map); a bare ≥95% threshold is never red, and where no such evidence is derivable red simply doesn't fire (honest absence). Token hexes unchanged (meanings renamed in place); idle (0-flow) belts keep their quiet render; MOTION = THROUGHPUT stays a fully orthogonal channel — under-used belts still trickle. — UI spec 1e flow grammar, SDD §5.2
